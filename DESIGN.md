# Design System: 知闲

## 1. Overview

浅色英语学习工作台。桌面侧栏与移动底部五项导航；对话主页、词书列表、连续阅读正文、句子列表和月历拥有不同的信息密度。优先使用现有图标与字体。

## 2. Colors

主操作绿色 #345c4b，深绿色 #244638。背景 #f7f9f8，正文表面 #ffffff，侧栏 #eef2ef，正文 #25352f，次级文字 #58675e，边界 #dce3dd。浅绿用于选中状态，橙色仅用于提醒。

## 3. Typography

界面使用 Noto Sans SC Variable 与 system-ui，正文 15px，辅助文本不低于 12px。英文文章使用 Newsreader Variable 与 Georgia，默认 23px、1.85 行距、约 68ch 行宽；用户可调整字号。界面标题 24–32px。

## 4. Elevation

表面以间距、背景和细边框分组。圆角 10–14px。主页面不使用装饰性大阴影。

## 5. Components

统一主要、次要和轻量按钮；按钮至少 40px 高，移动端关键操作 44px。抽屉使用原生 dialog，焦点闭环、Escape 关闭。普通列表使用分隔线，词书使用简洁封面加信息区。150–200ms 状态过渡，尊重减少动态效果设置。

## 6. Do's and Don'ts

- 保留文本选择与触摸系统菜单，选择操作栏不抢走选区。
- 使用标签、文字和图标表达状态，不能只依靠颜色。
- 不展示虚构学习统计；示例文章与收藏标注来源。
- 不采用装饰性数据大屏、重复卡片、科技感霓虹与大面积渐变。
