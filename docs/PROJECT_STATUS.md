# 项目当前状态

最后核对：2026-09-09。本次 AI 学习工作台变更基于提交 `1b8f4c5`。

## 1. 一句话状态

知闲当前是可本地运行、可通过 Docker Compose 部署的 AI 英语学习模块化单体。AI 对话是主页，词书、单词学习、连续阅读、句子收藏和学习日历已经形成一套完整工作台。固定来源、可审计的统一词典进入 Git，并在产品中按六个来源发布为独立系统预设词书。

FastAPI 应用版本为 `0.3.0`。当前不是静态原型：前后端、数据库迁移、测试、CI、容器和备份脚本均已存在。

## 2. 仓库与技术基线

| 项目 | 当前事实 |
|---|---|
| 主仓库 | `git@github.com:zhanghuiwan/zhixian.git` |
| 稳定分支 | `main`，GitHub 为唯一代码同步中心 |
| 前端 | Next.js 16.3、React 19.2、TypeScript 5.7、npm lockfile |
| 后端 | FastAPI 0.116、SQLAlchemy 2.0、Pydantic 2、Python 3.13 |
| 数据库 | 本地 SQLite；Docker/生产 PostgreSQL 17 |
| 迁移 | Alembic `0001` → `0002` → `0003` → `0004` → `0005`（当前 head） |
| 入口代理 | Nginx：`/` → Web，`/api/*` → API |
| CI | GitHub Actions：后端 pytest；前端 lint + build |
| 生产目标 | 阿里云 Ubuntu 单机 + Docker Compose，尚需域名/TLS 等实机参数 |

## 3. 已实现产品范围

### 第一阶段：学习闭环

- 邮箱密码注册、JWT 登录、个人资料、英语等级、每日新词量和时区。
- 公共词典与词书、当前词书选择、学习队列和学习进度。
- SM-2 风格的确定性复习算法与四档评分。
- 用户生词、多个生词本、来源、备注和熟练度。
- 公共文章、个人 AI 草稿、逐句中英阅读、查词和句子收藏。
- 今日数据、累计掌握、连续学习和最近活动仪表盘。
- 桌面和移动端应用壳、登录页及主要业务页面。
- AI 对话主页；词书、阅读、句子收藏和学习记录五项桌面/移动导航。
- 六本独立系统预设词书与个人词书统一入口，单词进度跨词书共享。
- 专注单词学习页、连续文章阅读、选区工具条、文章上下文 AI 和阅读位置恢复。
- 句子收藏搜索/筛选/笔记，以及按用户时区生成的月历和每日概况。

当前主要前端路由：

```text
/login              /register
/dashboard          /learn
/wordbooks          /vocabulary
/articles           /articles/[id]
/sentences          /records
/assistant          /profile
```

### 第二阶段：学习 Agent

- 用户级 DeepSeek、MiniMax 配置，服务端 Fernet 加密 API Key。
- SSE 流式对话、持久化会话/消息、Token 用量和工具审计。
- 学习历史、今日快照、未来计划预测、易错词和词典查询。
- 受限站内导航、多生词本操作、破坏性操作二次确认。
- 结构化例句、个人文章草稿和明确授权的长期学习偏好。
- 单 Agent、有界工具循环；当前没有子 Agent、向量库或跨会话自动检索。

实现细节见 `phase-2/AGENT_IMPLEMENTATION.md`。

## 4. 数据与测试现状

- 全部 ORM 实体集中在 `apps/api/app/models/entities.py`，共覆盖账号、学习、阅读、生词本、每日计划和 AI 会话域。
- `apps/api/app/db/seed.py` 提供 24 个兜底示例词和 5 篇原创示例文章；每位用户首次初始化 4 条示例句子收藏，示例删除后不会在重启或登录时复活。
- `zhixian-core-en-v1` 将六个来源的 21,739 条记录合并为 7,416 个唯一词典条目、19,372 条去重来源关联；产品只发布四级、六级、考研通用、考研英语一、考研英语二和雅思六本系统预设词书。
- 后端当前有 26 个测试函数，覆盖词库导入、用户隔离、个人词书、评分幂等、阅读进度、学习日历和示例收藏初始化。
- 前端当前依赖 ESLint、TypeScript 生产构建校验，尚无组件/端到端测试套件。
- CI 定义在 `.github/workflows/ci.yml`，对 `main` push 和 Pull Request 运行。

本次实测：Python 3.13 下后端测试通过（仅有 1 条 Starlette 依赖弃用警告）；前端 ESLint 和 Next.js 生产构建通过。SQLite 已验证空库直升 `0005` 和带旧收藏的 `0004 → 0005`；临时 PostgreSQL 17 已完成空库迁移、seed、7,416 词导入和 API 冒烟，得到 6 本系统预设词书、5 篇文章、4 条示例收藏。桌面端和 360px 页面已人工检查，浏览器无应用错误或 hydration 警告。

## 5. 尚未实现或明确暂缓

### 尚未实现

- PDF、扫描件 OCR、图片识别、TTS 缓存及持久化后台 worker。
- 邮箱验证、密码找回、第三方登录、后台管理、付费和社交功能。
- 跨会话全文/向量检索、长期记忆可视化管理、后台生成任务取消与补偿。
- 自动化生产发布流水线、域名和 HTTPS 证书配置。

### 明确暂缓

- “梧桐形象”相关的品牌角色、视觉资产与交互功能最后再开发。没有用户新指令不得提前展开。

## 6. 已知问题与风险

| 级别 | 项目 | 当前处理 |
|---|---|---|
| 发布前必须解决 | `compose.yaml` 将上传目录挂载到仓库内 `./data/uploads`，备份脚本默认读取 `/data/zhixian/uploads` | 正式启用上传前统一宿主机路径并做恢复演练 |
| 发布前必须解决 | 仓库 Nginx 只提供 HTTP | 公开登录前绑定域名并启用 HTTPS |
| 持续合规跟进 | qwerty-learner 仓库声明 GPL-3.0，词典还来自社区/第三方 | 项目所有者已批准当前 Git/部署用途；保留 GPL-3.0 副本和 `NOTICE.md`，更广泛分发前继续确认第三方义务 |
| 安全改进 | JWT 当前保存在浏览器 localStorage | 强化 CSP/XSS 防护；开放规模扩大时评估 HttpOnly Cookie 会话 |
| 质量改进 | 前端没有组件或 E2E 自动化测试 | 在复杂交互增长前引入关键路径测试 |
| 项目治理 | 知闲仓库当前没有 `LICENSE` 文件 | 在公开分发或接受外部贡献前由所有者选择许可证 |
| 运行限制 | Agent 在 API 进程内同步编排外部模型 | 耗时文件/音频任务必须使用后续 worker，不塞入 SSE 请求 |

## 7. 下一步推荐顺序

1. 推送当前提交后，在新目录从 GitHub 克隆并做一次最终冷启动冒烟。
2. 确认阿里云实例、域名、TLS 方案、生产挂载和异地备份，完成上线清单。
3. 在更广泛的公开分发或接受外部贡献前，决定知闲自身项目许可证并复核数据第三方义务。
4. 根据真实使用反馈再排第三阶段 PDF/OCR/TTS；梧桐形象继续留到最后。

## 8. 更新本文件的触发条件

功能上线、迁移新增、技术栈升级、生产部署状态变化、范围调整或已知风险关闭时，必须在同一变更中更新本文件。不要用预计完成时间冒充实际完成状态。
