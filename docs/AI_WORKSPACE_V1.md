# AI 学习工作台第一版

2026-09-08 用户确认先完成一个版本，并在数据库提供示例文章与句子收藏。

## 实施计划

1. 新增迁移：个人词书选择、评分来源/幂等、收藏快照、阅读记录、示例初始化标识。
2. 恢复六本系统预设词书；合并词库只作为底层导入载体保留，不再出现在产品入口。原来选择合并词书的用户迁移到四级词汇，单词掌握进度保持不变。个人词书复用 vocabulary_collections，避免破坏已有 Agent 工具。
3. 重做 AI 主页和五项导航；实现词书详情、学习模式、连续阅读及上下文 AI、句子管理、学习日历。
4. 原创示例文章按稳定 slug 幂等导入；每账号只初始化一次示例收藏，不伪造学习记录。
5. 验证后端、lint/build、SQLite/PostgreSQL 升级与桌面/360px 界面；同步文档。

## 兼容约定

词典 words 全局去重；系统词书 wordbooks 与个人词书 vocabulary_collections 在产品入口统一，以 kind + id 明确区分。用户进度仍按 user_id + word_id。旧评分来源显示未记录，不从当前归属猜测。JSONL、来源与系统预设留在 Git；用户内容保存在数据库。
